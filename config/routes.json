{
  "routes": [
    {
      "method": "GET",
      "path": "/check",
      "handler": "do-publish.check",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/publish",
      "handler": "do-publish.publish",
      "config": {
        "policies": []
      }
    }
  ]
}
